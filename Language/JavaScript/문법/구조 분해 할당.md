```
const colors = ['red', 'yellow', 'black'];
const Colors = {
    blue : 'blue',
    green : 'green',
    white : 'white',
}
// 배열에서 위치 별로 표시
const [red, yellow, black] = colors;
// 원하는 것만 표시
const [,yellow] = colors;
// 객체 타입 
const {blue, green, white} = Colors;
// 객체 타입 위치 변경 가능
```
